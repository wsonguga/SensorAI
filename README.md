# SensorAI
Sensor data science and AI tutorial

### Package installation
The installation of package "fastsst"  will encounter error if we put it into the requirement.txt. To install this package,
you should type "pip install fastsst --no-deps" in the command line.
